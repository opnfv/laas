/**
 * Since the tests are all loaded at the same time, anything declared globally
 * here will be able to be referenced by the other test scripts.
 */

const body = document.getElementsByTagName('body')[0];